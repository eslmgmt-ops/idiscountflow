import type { Metadata } from "next"
import type { ReactNode } from "react"

export const metadata: Metadata = {
  title: "Import database · idiscountflow",
}

export default function BulkUploadLayout({ children }: { children: ReactNode }) {
  return children
}
